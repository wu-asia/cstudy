#include<stdio.h>
#include<string.h>

int main()
{
	

	/*printf("%s\n", name);
	printf("%s\n", password);*/

	/*for (int i = 0; i < 3; i++)
	{
		char rightname[] = "zhangsan";
		char rightpassword[] = "1234qwer";

		char name[100];
		char password[100];

		printf("请输入您的用户名：\n");
		scanf("%s", name);

		printf("请输入您的密码：\n");
		scanf("%s", password);
		if (!strcmp(rightname, name) && !strcmp(rightpassword, password))
		{
			printf("通过验证。\n");
			break;
		}

		else
		{
			if (i == 2)
			{
				printf("次数已经使用完，验证失败。");
			}
			else
			{
				printf("你还有%d次机会。\n", 2 - i);
			}
		}
	}*/
	//见到你已经是我生命中最重要的时刻。
	// 爱你对我来说可能是一种奢望，我不求能与你在一起，我只希望我不会淹没在你的记忆长河之中

	
	/*printf("%s\n", str);
	int bigcount = 0;
	int smallcount = 0;
	int numbercount = 0;
	int othercount = 0;
	printf("请输入一串字符：\n");
	char str[100];
	scanf("%s", str);
	for (int i = 0; i < strlen(str); i++)
	{
		char c = str[i];
		if (c >= '0' && c <= '9')
		{
			numbercount++;
		}
		else if (c >= 'a' && c <= 'z')
		{
			smallcount++;
		}
		else if (c >= 'A' && c <= 'Z')
		{
			bigcount++;
		}
		else
		{
			othercount++;
		}
	}
		 printf("大写字母个数位%d\n",bigcount);
		 printf("小写字母个数位%d\n",smallcount);
		 printf("数字个数位%d\n",numbercount);
		 printf("其他字符个数位%d\n",othercount);

	return 0;
}*/